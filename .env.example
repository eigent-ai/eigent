# Public example-content catalog used by the default provider. These values are
# safe to expose in the renderer. Keep the feature disabled until the Workspace
# and Automation integrations are ready to ship.
VITE_EXAMPLE_CONTENT_ENABLED=false
VITE_EXAMPLE_CONTENT_CATALOG_URL=https://cdn.eigent.ai/catalog/index.json
